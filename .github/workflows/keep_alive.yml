name: Keep Render App Awake

on:
  schedule:
    # Runs every 12 minutes
    - cron: '*/12 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: curl -s https://x-paaf.onrender.com/
